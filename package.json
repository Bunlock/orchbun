{
  "name": "orchbun",
  "version": "0.2.0",
  "description": "Token-efficient orchestration for Codex, Claude Code, and OpenRouter agents",
  "private": true,
  "type": "module",
  "bin": {
    "orchbun": "dist/cli.js",
    "orchbun-mcp": "dist/mcp.js"
  },
  "files": [
    "dist",
    "schemas"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "mcp": "node dist/mcp.js",
    "test": "node --test --import tsx test/**/*.test.ts",
    "check": "tsc -p tsconfig.json --noEmit && node --test --import tsx test/**/*.test.ts"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "ajv": "^8.17.1",
    "yaml": "^2.8.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2"
  },
  "license": "MIT"
}
